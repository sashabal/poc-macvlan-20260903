services:
  app:
    build: .
    ports:
      - "8080:8080"
    networks:
      - escape
      - default

networks:
  escape:
    driver: ipvlan
    driver_opts:
      parent: eth0
      ipvlan_mode: l3
